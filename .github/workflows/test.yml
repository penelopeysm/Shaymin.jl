name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        uses: penelopeysm/Shaymin.jl@main
        with:
          paths: |
            path1
            path2
            path3
