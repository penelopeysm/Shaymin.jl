on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Fail
        run: exit 1

      - name: Echo
        if: always()
        run: echo "Hello, world!"
